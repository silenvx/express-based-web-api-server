# express-based-web-api-server

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```

## docker

To create dir for mount volume:

```
mkdir -p data
```

To run:

```
docker compose up -d
```
